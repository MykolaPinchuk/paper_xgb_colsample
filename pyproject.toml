[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "paper-xgb-colsample"
version = "0.0.0"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["xgb_intratree_colsamp_ratios"]
